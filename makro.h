#ifndef MAKRO_H
#define MAKRO_H

#include <ctype.h>

/*
   General Makros
*/

#define isalnum1(x) ( isalnum(x) || (x) == '_')
#define isalpha1(x) ( isalpha(x) || (x) == '_')

#endif // #ifndef MAKRO_H
