language: c

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y gcc
  - sudo apt-get install libncurses5-dev
  - sudo chmod +rwx /usr/local/lib/xwpe
  
script:
  - ./configure
  - make
  - make install
  - make clean
  
